// SPDX-License-Identifier: MIT
pragma solidity >=0.8.0;

library LibReinforce {
  function reinforce(
    bytes32 playerEntity,
    bytes32 rockEntity,
    bytes32 arrivalId
  ) internal {
    //  Arrival memory arrival = ArrivalsMap.get(arrivalId);
  }
}
